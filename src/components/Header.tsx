import { AppBar, Toolbar } from "@mui/material";
import React from "react";

const Header = () => {
  return (
    <AppBar>
      <Toolbar>
        <h2>Bucchus</h2>
      </Toolbar>
    </AppBar>
  );
};

export default Header;
